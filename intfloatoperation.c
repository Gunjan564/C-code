#include<stdio.h>
int main()
{
    /*
    == CASE 1(if both operrands are either int or float then
    the output will be stored only in int or float 
    respectively.Even if the assigned values are in 
    decimal the output will be in the input format 
    specifier format. If the operation include another
    term(decimal or real number)then the output will
    depend on output format specifier).
    NOTE:- 5/2 is accepteble in integer but 5.2 is not
    ==CASE 2(if the input values are mixed between int and float then
    the output will depend on output format specifier)*/
    
    
}