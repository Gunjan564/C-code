#include<stdio.h>
#include<string.h>
int main()
{
    int n,*ptr;
    printf("Enter the number of number of characters in your id: ");
    scanf("%d",&n);
    struct employ
    {
        char id[];
    }
}